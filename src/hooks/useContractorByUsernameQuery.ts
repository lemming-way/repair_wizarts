// todo: В настоящий момент здесь просто заглушка. Нужно переписать или избавиться от этого хука

export function useContractorByUsernameQuery(username: string | undefined) {
  return { data: {} };
}
